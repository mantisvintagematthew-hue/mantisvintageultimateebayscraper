2026-02-14
- Initialized project scaffold for vintage-tag-catalog.
- Added Typer CLI with commands: collect, fetch-images, pick-images, extract, date, export.
- Implemented eBay auth + browse API clients.
- Implemented SQLite SQLAlchemy schema and repository helpers.
- Implemented pipeline steps for collect/download/select/extract/infer/resolve/export.
- Added OpenCV-based tag and hero image ranking heuristics and stitch detector.
- Added EasyOCR-based tag OCR module.
- Added unit tests + fixtures for date extraction, resolver, and image rankers.
- Added Dockerfile, devcontainer config, docker-compose, Makefile, env example.
- Added project README with setup and usage instructions.
2026-02-15
- Added paginated collect behavior to honor limits above single-page eBay response sizes.
- Added offline fixture collection mode (`vtc collect --fixture ...`) for local/CI runs without API credentials.
- Improved image URL extraction, URL dedupe, extension inference, and HTTP error handling in image downloads.
- Updated extraction pipeline to parse listing specifics (`localizedAspects`) from raw JSON and feed them into declared date extraction.
- Expanded tests with offline collection test and specifics-based declared-date parsing test.
2026-02-15 (production readiness pass)
- Added eBay item detail enrichment during collect (`get_item`) to persist richer payloads than summary-only records.
- Added retry/backoff behavior for transient eBay Browse API failures (429 and 5xx).
- Added `vtc doctor` preflight command for direct tester readiness checks (db + credentials + data dir).
- Switched schema/repo timestamp defaults to timezone-aware UTC helper to avoid deprecated naive utcnow usage.
- Added tests for collect enrichment behavior with/without detail fetch.
- Expanded README/Makefile with direct API test flow and doctor command.
2026-02-15 (readiness iteration 2)
- Added `vtc run-all` orchestration command for direct tester end-to-end runs.
- Updated fixture collection to honor `--limit` for deterministic offline runs.
- Tightened collect enrichment error handling to catch/log HTTP errors explicitly.
- Improved export serialization to avoid leaking ORM private state internals.
- Added fixture-limit test coverage and updated README/Makefile with run-all + readiness checklist.
2026-02-15 (human-testing readiness pass)
- Added structured logging with run IDs and persisted per-run metrics artifacts under `data/runs/`.
- Added `vtc metrics` and `vtc qa-sample` commands for operational monitoring and manual QA workflows.
- Added retry/backoff tuning in image download and OCR stages with explicit failure counters/logging.
- Expanded brand/region normalization and inference signals for broader real-world vintage listing coverage.
- Added realism regression fixtures and expanded unit tests for normalization/inference/resolver edge cases.
- Updated README and Makefile with live 500-listing validation protocol and QA rubric thresholds.
